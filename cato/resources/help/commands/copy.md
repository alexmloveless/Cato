# /copy

Copy the last assistant response to the system clipboard.

## Usage
- `/copy`

## Notes
Cato will try common clipboard utilities depending on your system (for example: `wl-copy`, `xclip`, or `xsel`).